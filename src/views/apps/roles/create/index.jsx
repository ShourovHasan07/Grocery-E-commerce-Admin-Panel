// MUI Imports
import Grid from "@mui/material/Grid2";

import CreateForm from "./CreateForm";

const RoleCreate = () => {
  return (
    <Grid container>
      <Grid size={{ xs: 12 }}>
        <CreateForm />
      </Grid>
    </Grid>
  );
};

export default RoleCreate;
